<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="SpotiLove.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:SpotiLove"
    Title="SpotiLove"
    Shell.FlyoutBehavior="Disabled">

    <!-- Main App Pages -->
    <ShellContent
        ContentTemplate="{DataTemplate local:MainPage}"
        Route="MainPage" />

    <ShellContent
        ContentTemplate="{DataTemplate local:Login}"
        Route="Login" />

    <ShellContent
        ContentTemplate="{DataTemplate local:SignUp}"
        Route="SignUp" />

    <ShellContent
        ContentTemplate="{DataTemplate local:CompleteProfilePage}"
        Route="CompleteProfilePage" />

    <ShellContent
        ContentTemplate="{DataTemplate local:ArtistSelectionPage}"
        Route="ArtistSelectionPage" />

</Shell>